package model.expressions;

/*
 * This is the same as in the case for ArithmeticOperator
 * We use this to make our life easier and make code easier to read.
 * LogicalOperator.AND, LogicalOperator.OR
 */
public enum LogicalOperator {
    AND, OR
}
